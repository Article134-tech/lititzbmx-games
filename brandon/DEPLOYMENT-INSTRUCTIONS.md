# Deployment Instructions

The game is deployed as the `/brandon/` sibling experience inside the `Article134-tech/lititzbmx-games` GitHub Pages repository.

1. Confirm GitHub Desktop is on repository `lititzbmx-games` and branch `main`.
2. Copy the patch **contents** into the repository root, preserving the `brandon/` paths.
3. Confirm only the documented patch files changed.
4. Commit: `Add mid-game restart controls to Brandon v1.1.1`
5. Push to `main`; GitHub Pages will redeploy the same public URL automatically.
6. Verify restart, return-to-introduction, progress saving, all nine stages, source links, outcomes, mobile layout, and print/save-as-PDF output.

Public URL remains:

`https://article134-tech.github.io/lititzbmx-games/brandon/`

Do not present the hosted game as an official township tool or approval process.
